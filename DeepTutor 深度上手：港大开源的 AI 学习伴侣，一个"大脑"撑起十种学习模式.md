# DeepTutor 深度上手：港大开源的 AI 学习伴侣，一个"大脑"撑起十种学习模式

## 摘要（可直接粘进公众号摘要框）

港大团队开源的 DeepTutor，把聊天、出题、研究、阅读、记忆全部跑在同一个智能体循环上。我装了一个，接上 DeepSeek，拿高中物理的电荷知识点走了一轮精通之路，再把官方文档逐页读了一遍。这篇讲清楚它怎么装、怎么用、哪些地方想得比同类细，以及和 NotebookLM、Khanmigo 的差别。

---

## 正文

DeepTutor 官方文档里有一句自我介绍，我看了两遍：聊天主页"不是一个普通的输入框，而是默认的智能体循环"。

多数 AI 学习产品做到聊天框就收工了。这个港大团队的开源项目想得更远——聊天只是同一个大脑的其中一扇门，出题、研究、读书、记忆，走的是同一条路。项目 2025 年 12 月底发布，39 天拿到 1 万颗 star，现在 GitHub 仓库是 3.87 万颗。

我把它装起来了，也拿高中物理试了一轮。下面按"装好—用一遍—看懂它"的顺序讲。

## 一、先装起来，二十分钟的事

环境要求写在文档首页：Python 3.11 到 3.14，Node.js 20 以上。剩下的就三行命令：

    pip install -U deeptutor
    deeptutor init
    deeptutor start

`init` 会问你端口、模型提供商、要不要配 Embedding 和搜索；`start` 起两个服务，FastAPI 后端在 8001，Next.js 前端在 3782，浏览器打开 `http://localhost:3782/` 就能看到界面。除了 PyPI，官方还给源码、Docker、纯 CLI 三条路，Docker 那条只暴露 3782 一个端口就够，前端会在容器内部把 API 请求转给后端。

模型不绑厂商。云端 API 填 key 就行，Ollama、LM Studio、vLLM 这类本地模型不用 key 也能跑。我接的是 DeepSeek V4：

图 1：配置了 DeepSeek V4 的 DeepTutor 首页
![配置了 deepseek-v4 的 DeepTutor](Pasted image 20260905231726.png)

装完先看清它有哪些模式。Home 页一键可达四种：聊天、主动提问、测验、可视化。

图 2：Home 页的能力选择器
![Home 页能力选择器](Pasted image 20260905232115.png)

解题、综合研究、YouTube 视频学习收在"更多能力"里；精通之路和沉浸式阅读不占 Home 的位置，是左侧栏里独立的工作区。模式各有各的样子，底层跑的是同一个循环。

图 3：更多能力里的解题、研究与视频学习
![更多能力](Pasted image 20260905232222.png)

想扩展也不用改核心代码：45 个精选 MCP 服务、101 个命令行工具、社区 Skill 市场 EduHub，都在学习空间里按需开启。

## 二、拿高中物理电荷走一轮精通之路

光看文档不算用过。我建了一条精通之路，目标是搞懂高中物理的电荷。

第一步填目标，AI 自己规划学习模块和知识点，我改了两处顺序就确认了：

图 4：输入学习目标后自动生成的模块规划
![学习目标与模块规划](Pasted image 20260905233412.png)

第二步进入学习界面。左边是当前主题和进度环，右边是对话，中间那份大纲可以折叠：

图 5：精通之路的学习工作区
![开始学习](Pasted image 20260905233526.png)

从大纲往下点，每个知识点单独开一轮：

图 6：从大纲进入具体知识点
![从大纲开始深入学习](Pasted image 20260905233701.png)

真正的区别在这儿：它给每个知识点标了类型——记忆、流程、概念、设计，类型决定拿什么证据才算掌握。而且设了硬门槛，当前这个点没过关，下一个不开放；已经过了的点，到期还会通过间隔复习再冒出来问你一遍。

图 7：逐个知识点深入，附练习与讲解
![深度学习各个知识点](Pasted image 20260905233837.png)

进度条走的不是"你读了多久"，是"你答对了没有"。做完一轮，答错的题会连同我的作答、参考答案和解析一起进题库，可以只看错题复习。学过的对话按主题归档在侧栏，重开还在原来的位置。

这套东西适合备考，也适合补一门落下的课。它不哄你。

## 三、读书这件事，它要求"有依据"

沉浸式阅读把文档和对话并排放，中间的分隔条能拖。助手读的就是你看到的那些单元——PDF 的物理页、EPUB 的章节、演示文稿的幻灯片——引用统一写成 `[p.N]`，只有跟工具返回结果核实过的定位才变成链接，点一下阅读器跳过去，还会高亮那句原文。

最见功力的是一个很笨但很有效的工程决定：模型开跑之前，DeepTutor 先拿你的问题在打开的文档里搜一遍，把命中的几段直接塞进这一轮的上下文。

文档里说得挺直白：弱一些的模型在原生工具调用下常常一次读取工具都不调，这样做至少保证它从对的来源单元开始。而且这次前置检索是普通搜索，不是第二轮 LLM，不额外花 token，也不推迟第一个字出来。

检索还分了层，先精确匹配，再规范化匹配（压掉空白和标点差异，所以从 PDF 里复制出来的引文哪怕中间断了行也能对上），最后才按词项重合度排序。

读完的成果能带走：高亮和批注可以导出成真正的 PDF 批注，换到预览、Acrobat、Chrome 里打开，标记都还在；也能导成 Markdown 按页码顺序列出。单个文档上限 200 MB，PDF、EPUB、Word、表格、幻灯片、Markdown、音视频、网页链接、YouTube 和 Bilibili 都收，但纯图片的扫描件会被明确拒绝——它宁可告诉你先去 OCR，也不给你开一个空白阅读器。

![沉浸式阅读：文档与辅导对话并排](https://docs.deeptutor.info/screenshots/guide/reading/00-overview.png)

## 四、把材料攒成资产

知识库是 RAG 背后那堆文档，但引擎不绑死一家：LlamaIndex、PageIndex、GraphRAG、LightRAG、腾讯 IMA、MarginNote 4、Obsidian 库都能选，一个库绑一种引擎。已经有索引的可以"关联已有"，就地读取，不上传不重建。知识库还能跟 GitHub 仓库或有界的文档站点，同步时对增删改做差异对账。

重建索引会写进新的 `version-N` 目录，旧的留着。也就是说重建到一半挂了，你手上那个能用的索引不会被毁掉。文档解析引擎有七种可选，本地模型下载默认是关的，要不要下由你点头。

再往上一层是"活书"。它把知识库、笔记本、题库或者聊天历史编译成一本能操作的书：一节内容变成测验块，一段推导变成 Manim 动画，一个过程变成能拖滑块的交互组件，每页旁边还有一个只针对这一页的 Page Chat。编译前先给你章节提案和逐章的成本预估，你审过了它才开跑，不是闷头生成一整本再让你收货。

![活书：嵌入页面的测验块](https://docs.deeptutor.info/screenshots/guide/book/01-book-demo-quiz-card.png)

## 五、它记得你，而且你能查账

记忆这块是我最认可的设计。DeepTutor 刻意没做成一个隐藏的向量库，而是三层能打开来看的文件：L1 是工作区的实时镜像加只追加的变更记录，L2 按界面整理事实，L3 做跨界面综合出画像、近况、范围和偏好。每条结论旁边都写着它引用了哪一层、哪个实体。

记忆图谱把三层画成同心圆，中心是 L3 综合，中环是 L2 事实，外圈是 L1 实体，悬停能预览，点击能锁住高亮连线。它觉得你哪里理解得不对，你能顺着图查到它是从哪条记录得出这个判断的。

![记忆图谱](https://docs.deeptutor.info/screenshots/guide/memory/01-3-layer-memory-graph.png)

文档里给的建议也很实在：长周期的学习就把记忆开着；哪天觉得它语气或者假设不对劲，去看 L3。

## 六、把这个导师接到微信里

伙伴（Partners）是同一个大脑的另一种形态。每个伙伴有自己的灵魂文件 SOUL.md、模型策略、资料库、记忆和渠道配置，可以接个人微信、企业微信、QQ/NapCat、飞书、钉钉、Telegram、Slack、Discord 等十几个渠道。

官方文档里有个定义我很喜欢：伙伴是"一段有了人格、还带了手机号的聊天"。渠道只是投递适配器，网页里和微信里跟它说话，推理走的是同一条路，记忆也是同一条。

有个细节挺贴心：渠道卡片上有"发送进度"开关，因为 IM 里沉默 60 秒，对面的人会以为它坏了。

聊天里还能用 `cron` 排期。"工作日每天早上九点就这一章考我"——这是一句话，不是一个设置页面。到点的任务会顺着伙伴连的那个 IM 渠道发回来。

## 七、跟 NotebookLM、Khanmigo 比一比

这个领域有两个标杆。

NotebookLM 主打来源导向，只基于你上传的资料回答。据 PCMag 报道，今年 3 月谷歌给它加上了闪卡和测验，往个人导师的方向走；不过 buildmvpfast 的评测仍把它定位成资料锚定型学习助手，不是通用导师。DeepTutor 的沉浸式阅读和知识库思路相近，但引用核实做得更细——定位到 PDF 物理页码，批注能导出成真正的 PDF 批注，这两点 NotebookLM 目前没有。

Khanmigo 站在教学法的另一极：苏格拉底式引导，不直接给答案，围着可汗学院自己的内容库做 K-12 辅导。据微软官方学习平台介绍，微软和可汗学院合作，给 40 多个国家的英语教师提供免费访问。但它闭源、中心化，数据和模型都不在你手里。

DeepTutor 的差异就四个字：开源自托管。模型自己挑，数据留在本地磁盘，还能接进你自己的 IM 生态，Apache 2.0 协议。代价是要自己部署、自己配模型、自己掏 API 费用，换来的是完全的控制权。

## 结语

DeepTutor 想做的不是"更聪明的聊天框"，而是一套能长期经营的学习系统：读过的文档、攒下的笔记、做错的题，都会变成下一轮对话的依据，而且这些依据你能一条条查。它把导师装进了微信，也把你的学习痕迹留在了你自己的硬盘上。

对教育智能体感兴趣的朋友欢迎联系探讨。郑州地区有本地化部署需求的，我可以免费上门部署。

---

## 附：几句值得留着的原文

> "伙伴是一段有了人格、还带了手机号的聊天。"
> "A partner is 'a chat that has a personality and a phone number.'"

> "记忆是一套基于文件的三层系统，可读、可整理、可审计——它刻意不是隐藏的向量库。"
> "Memory is a file-backed, three-layer system you can read, curate, and audit — deliberately not a hidden vector store."

> "DeepTutor 是一个智能体原生的学习工作区，把辅导、解题、出题、研究、可视化与掌握度练习连进同一个可扩展的系统。"
> "DeepTutor is an agent-native learning workspace that connects tutoring, problem solving, quiz generation, research, visualization, and mastery practice in one extensible system."

---

原文链接：https://docs.deeptutor.info/zh-cn/
GitHub 仓库：https://github.com/HKUDS/DeepTutor
arXiv 论文：https://arxiv.org/abs/2604.26962
文中远程配图来自 DeepTutor 官方文档站，图 1 至图 7 为本人实操截图。
