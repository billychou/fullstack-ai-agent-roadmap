<section>

## 1. Harness 是什么：四层模型中的执行层

**论点**：Harness 是模型之外的一切工程代码，与编排框架（LangChain/LangGraph）分属不同层级，混为一谈是当前对比讨论中最大的概念错误。

**表格1**：展示模型—Harness—框架—平台四层分工及各层代表产品

**写作指导**：先引 LangChain 官方定义与四层模型，再区分"框架组合多个 Agent、Harness 运行单个 Agent"，最后点出 LangChain 自己对框架/运行时/Harness 的三分。

</section>

<section>

## 2. 四款产品架构横向对比

### 2.1 Claude Code 与 Codex：闭源成品路线

**论点**：Claude Code 与 Codex 是把 Harness 封装为订阅制成品的代表，模型绑定与不可拆解是其结构性代价。

**写作指导**：并列分析两者的许可、技术栈、模型锁定与生态位，引用 winder 实测评价（Claude Code 记忆设计偏薄、Codex 沙箱优先）。

### 2.2 DeepSeek Harness：一切皆插件的开源运行时

**论点**：DSH 是唯一把主循环也做成可替换插件的 Harness，代价是开发者预览期的全面不稳定。

**表格2**：展示九款主流 Harness 的厂商、许可、技术栈、适用场景与主要弱点

**写作指导**：聚焦 DSH 的插件化粒度差异，说明其与 Claude Code 是"整车与乐高"之别，并标注开发者预览风险。

### 2.3 LangChain/LangGraph：编排框架而非 Harness

**论点**：LangGraph 用显式状态机换确定性与可恢复性，与 DSH 的配置组合哲学构成两条路线之争。

**表格3**：展示 47 小时对照实测中两者在延迟、内存、成功率、人机协同开发成本上的差异

**写作指导**：对比两种哲学（组合优于配置 vs 显式优于隐式），用实测数据说明各自胜负面，标注该数据为单源且基于 rc.6。

</section>

<section>

## 3. 换 Harness 比换模型更影响结果

**论点**：同一模型在不同 Harness 下表现差异远超模型间差异，Harness 工程已成为 Agent 能力的主要变量。

**写作指导**：列举 Vercel 删工具、LangChain 改 Harness 提分、CORE-Bench 同模型双分数、Letta 反超 Claude Code 等案例，并引 Raschka 的限定条件。

</section>

<section>

## 4. 工具层视觉：纯文本模型的看图降级链路

**论点**：DSH 在模型不支持图像时自动退化为工具层视觉，用 OCR 与像素分析把图片拆成结构化证据，这是 Harness 能力补齐模型短板的典型样本。

**写作指导**：说明该术语无权威定义、实为社区对 rc.8 多模态降级机制的概括，展开其实现路径与设计动机，顺带说明与原生视觉模式的切换关系。

</section>

<section>

## 5. 子代理调度：五种接缝与统一调度层野心

**论点**：DSH 通过五种子代理接缝把 Claude Code 与 Codex 收编为可插拔组件，正在从单个 Agent 滑向 Agent 调度层。

**写作指导**：列出进程内派生、进程内分叉、ACP、Codex、Claude Code 五种提供方，结合 rc.8 的 Profile Bundle、非交互权限、reportDelivery 与 Job Panel 说明调度闭环。

</section>

<section>

## 6. 缓存经济学：命中率决定生死

**论点**：DeepSeek 定价中缓存命中与未命中价差约 30 倍，Harness 的上下文工程直接决定 Agent 运行成本，这解释了 DSH 事件溯源架构的经济动机。

**表格4**：展示 V4-Flash 与 V4-Pro 缓存命中/未命中价格及价差

**写作指导**：先列定价数据（注明时点与口径差异），再对比各 Harness 缓存命中率（Claude Code 1.5%、Codex 约 70%、DSH 约 99%），最后落到事件溯源保证前缀稳定的机制。

</section>

<section>

## 7. Cordis 论文：时空可组合性的形式化

**论点**：Cordis 论文把插件副作用的可逆性从工程约定提升为运行时形式化保证，是理解 DSH 热插拔与时间回退能力的理论根基。

**写作指导**：拆解论文核心概念——可逆效应、响应式共效应、统一上下文类型、动态组合演算，并说明 Cordis 实现（效应追踪、共效应解析、配置调和、热替换）与 VS Code 插件审计的对照。

</section>

<section>

## 8. 安全：一次沙箱逃逸与一长串信任模型缺口

**论点**：DSH 两周内的安全图景是"一个已修复的 Bubblewrap 逃逸 + 多项未证实的社区报告缺口"，反映开发者预览期信任边界尚未建立。

**表格5**：展示社区报告的信任模型缺口清单及对应风险

**写作指导**：先讲已确认的 /proc/<pid>/root 逃逸修复与文档站供应链修复，再列社区报告缺口并强调均未独立证实，最后给出防护清单并横向提及 Claude Code 同期 CVE 说明这是行业共性问题。

</section>

<section>

## 9. 插件分发层：从仓库堆到可发现市场

**论点**：四天内插件仓库从 700 涨到近 8000，索引站、精选目录与桌面分发层同步出现，但接口不稳定与定价波动正在给生态收税。

**写作指导**：按"内容—策展—分发"三层盘点生态基础设施，用 dsh-anchored-standard 停更案例说明定价对评估密集型项目的系统性风险，引 Remio 关于契约稳定的判断收尾。

</section>

<section>

## 10. 选型结论：按层级与假设选择而非按功能清单

**论点**：四款产品分别占据成品订阅、开源自托管、编排框架三个生态位，选型应基于模型绑定、部署位置与行为可控性三个假设，而非功能对比表。

**表格6**：展示按使用场景给出的一句话选型建议

**写作指导**：先重申"按假设对比不过期、按功能对比会过期"，再给出分场景选型建议，最后以双持策略与观察期建议收尾。

</section>
